postgre_common_config: &postgre_common_config
  adapter: postgresql
  encoding: unicode
  pool: 5
  username: postgres # if using default PostgreSQL user then the value should be: postgres
  password: root
  host: 127.0.0.1 # If the DB is setup on local machine then use: localhost

development:
  database: s_app_development
  <<: *postgre_common_config

# Warning: The database defined as "skill_hunt_test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  database: s_app_test
  <<: *postgre_common_config

production:
  database: s_app_production
  <<: *postgre_common_config

staging:
  database: s_app_staging
  <<: *postgre_common_config